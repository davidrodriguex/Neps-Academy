#include <stdio.h> // Inclui a biblioteca Standard Input-Output

int main (){ // Declara a função principal
    // Código a ser executado pela função principal.
    printf("Ola Neps Academy!\n"); // função de impressão na tela
    printf("Ola,\nNeps Academy"); // \n é parametro para quebra de linha
    return 0; // Retorna 0 para indicar que o programa terminou com sucesso
}

// Status de submissão dos exercícios:
    //AC(Accepted) - Aceito, resposta correta;

    //WA(Wrong Answer) - Resposta Errada, problema de lógica;
    //TLE(Time Limit Exceeded) - Tempo Limite Excedido, problema de eficiência;
    //CE(Compilation Error) - Erro de Compilação, problema de sintaxe;
    //RE(Runtime Error) - Erro de Execução, acesso a memória inválido.